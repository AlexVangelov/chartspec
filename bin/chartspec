#!/usr/bin/env ruby

require 'rspec'
require 'chartspec'

#RSpec::Core::Runner::run ARGV + ["--format", "Chartspec::Formatter", "--out", ENV["CHARTSPEC_HTML"] || "tmp/chartspec.html"]
code = RSpec::Core::Runner::run ARGV + ["--format", "Chartspec::Formatter"]
exit(code) unless code == 0